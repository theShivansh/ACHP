# ============================================================
# ACHP v2 — Python Backend Requirements (apps/api/)
# Answer · Complete · Honest Probe
# ============================================================
# Install: pip install -r requirements.txt

# --- Web Framework + SSE ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
sse-starlette>=2.1.0              # Server-Sent Events for real-time UI
python-multipart>=0.0.9

# --- LLM Providers ---
groq>=0.9.0                       # Groq SDK (Llama 4 Scout)
openai>=1.30.0                    # OpenRouter uses OpenAI-compatible API

# --- Async & Orchestration ---
aiohttp>=3.9.0
anyio>=4.0.0
asyncio-pool>=0.6.0
tenacity>=8.3.0                   # Retry with exponential backoff

# --- Semantic Cache ---
redis>=5.0.0
fakeredis>=2.21.0                 # In-memory Redis (dev/test)

# --- Embeddings & Reranking ---
sentence-transformers>=3.0.0      # all-MiniLM-L6-v2 + cross-encoders
torch>=2.2.0
transformers>=4.40.0

# --- Retrieval ---
rank-bm25>=0.2.2
faiss-cpu>=1.8.0
duckduckgo-search>=6.0.0

# --- NLP / NIL Sub-agents ---
vaderSentiment>=3.3.2
nltk>=3.8.0
spacy>=3.7.0

# --- Data Validation ---
pydantic>=2.7.0
pydantic-settings>=2.0.0

# --- Configuration ---
python-dotenv>=1.0.0
pyyaml>=6.0.0
omegaconf>=2.3.0

# --- Security ---
presidio-analyzer>=2.2.0
presidio-anonymizer>=2.2.0

# --- Logging ---
structlog>=24.0.0
rich>=13.7.0

# --- HTTP ---
httpx>=0.27.0

# --- Synthetic Data (Post-training) ---
datasets>=2.19.0
accelerate>=0.29.0

# --- Notebooks ---
jupyter>=1.0.0
ipykernel>=6.29.0
matplotlib>=3.9.0
seaborn>=0.13.0
pandas>=2.2.0
numpy>=1.26.0

# --- Testing ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0

# --- Dev Tools ---
black>=24.0.0
isort>=5.13.0
mypy>=1.10.0
ruff>=0.4.0


# ============================================================
# ACHP v2 — Node.js / pnpm Frontend Dependencies (apps/web/)
# Install: pnpm install (from monorepo root or apps/web/)
# ============================================================
#
# Core Framework
#   next@15.x
#   react@19.x
#   react-dom@19.x
#   typescript@5.x
#
# Styling
#   tailwindcss@4.x
#   @tailwindcss/vite
#   postcss
#   autoprefixer
#
# Component Library
#   shadcn/ui  (via: pnpm dlx shadcn@latest init)
#   @radix-ui/react-*  (installed by shadcn)
#   class-variance-authority
#   clsx
#   tailwind-merge
#
# Animation
#   framer-motion@11.x
#
# Charts
#   recharts@2.x           ← RadarChart for BIS/PCS/EPS/NSS/CTS
#
# Icons
#   lucide-react
#
# Fonts
#   next/font (Inter + JetBrains Mono — via Google Fonts)
#
# Theme
#   next-themes             ← Dark mode provider
#
# HTTP / State
#   @tanstack/react-query@5.x
#   axios
#
# Dev / Testing
#   @playwright/test@1.x    ← E2E tests
#   eslint@9.x
#   prettier@3.x
#   @types/node
#   @types/react
#   @types/react-dom
